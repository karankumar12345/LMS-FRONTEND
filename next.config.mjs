/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com', 'i.pinimg.com','example.com','encrypted-tbn0.gstatic.com'],

       
      },
    
};

export default nextConfig;
